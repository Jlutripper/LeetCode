package com.jlutripper.leetcode.everyday;

import com.jlutripper.leetcode.ds.search.BiSearch;

import java.util.ArrayList;
import java.util.List;

/**
 * @description: 658. 找到 K 个最接近的元素
 * @author: Jlutripper
 * @create: 2022-08-25 21:13
 **/


public class FindClosestElements {
    /**
     * @Param: [int[], int, int]
     * @return: java.util.List<java.lang.Integer>
     * @Description: 给定一个 排序好 的数组 arr ，两个整数 k 和 x ，从数组中找到最靠近 x（两数之差最小）的 k 个数。返回的结果必须要是按升序排好的。
     * @Author: Jlutripper
     * @Date: 2022/8/25 21:22
     */

//    public List<Integer> findClosestElements(int[] arr, int k, int x) {
//        List<Integer> ans = new ArrayList<>();
//        int len = arr.length;
//        if (arr[0] >= x) for (int i = 0; i < k; i++) ans.add(arr[i]);
//        if (arr[len - 1] < x) for (int i = len - k; i < len; i++) ans.add(arr[i]);
//
//
//    }
}
